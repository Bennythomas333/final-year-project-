  🛡️Network Intrusion Detection System (NIDS) using Machine Learning

...A machine-learning-based NIDS that detects network intrusions using an ensemble of Random Forest, XGBoost, and LightGBM, combined through soft-voting. 

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Bennythomas333/final-year-project-/blob/main/notebooks/NIDS_Full_Pipeline.ipynb)
[![GitHub](https://img.shields.io/badge/GitHub-Repo-black?logo=github)](https://github.com/Bennythomas333/final-year-project-)

> Train, evaluate, and demo an ML-based Network Intrusion Detection System — entirely in Google Colab. No local setup required.

## 🚀 One-Click Start

1. Click **"Open in Colab"** badge above
2. Runtime → **Change runtime type → GPU** (optional, speeds up training)
3. Run All Cells (`Ctrl+F9`)

## 📁 Project Structure

```
nids-ml/
├── notebooks/
│   └── NIDS_Full_Pipeline.ipynb   ← Main Colab notebook (all-in-one)
├── src/
│   ├── preprocess.py              ← Data pipeline (importable in Colab)
│   ├── train.py                   ← Model training
│   ├── predict.py                 ← Inference helper
│   └── evaluate.py                ← Metrics & plots
├── dashboard/
│   └── index.html                 ← GitHub Pages dashboard
├── requirements_colab.txt         ← Colab-compatible deps
└── README.md
```

## 🔗 Links
- **Colab Notebook**: Run everything in the cloud
- * **Dashboard**: https://bennythomas333.github.io/final-year-project-/dashboard/index.html
- **Dataset**: https://www.unb.ca/cic/datasets/ids-2017.html (or use synthetic data auto-generated by notebook)

## 📊 Results

| Model         | Accuracy | Macro F1 | ROC-AUC |
|---------------|----------|----------|---------|
| Random Forest | —        | —        | —       |
| XGBoost       | —        | —        | —       |
| LightGBM      | —        | —        | —       |
| **Ensemble**  | **0.9678** | **0.9608** | **0.9914** |

> Confusion Matrix (Ensemble on 11,998 test flows): TN=8,343 · FP=57 · FN=329 · TP=3,269

## 🧠 Models & Hyperparameters

**Random Forest**
- `n_estimators=200`, `max_depth=20`, `min_samples_split=5`, `class_weight='balanced'`

**XGBoost**
- `n_estimators=200`, `max_depth=8`, `learning_rate=0.1`, `subsample=0.8`, `colsample_bytree=0.8`

**LightGBM**
- `n_estimators=200`, `max_depth=8`, `learning_rate=0.1`, `subsample=0.8`, `colsample_bytree=0.8`

**Ensemble**
- `VotingClassifier(voting='soft')` combining all three above

---

## 🚦 Attack Classes (15 total)

`BENIGN` · `DoS Hulk` · `PortScan` · `DDoS` · `DoS GoldenEye` · `FTP-Patator` · `SSH-Patator` · `DoS Slowloris` · `DoS Slowhttptest` · `Bot` · `Web Attack Brute Force` · `Web Attack XSS` · `Web Attack Sql Injection` · `Infiltration` · `Heartbleed`

---

## 🚀 How to Run

### Option 1 — Google Colab (Recommended)

1. Open the notebook in Colab
2. Runtime → **Change runtime type → GPU (T4)**
3. Run all cells top to bottom
4. Use the interactive widget at the bottom to test live predictions

### Option 2 — Real CICIDS-2017 Dataset

1. Set `USE_REAL_DATASET = True` in Cell 2
2. Mount Google Drive and place CSV files in `/content/drive/MyDrive/CICIDS2017/`
3. Run all cells

### Dependencies (auto-installed in Colab)

```bash
pip install lightgbm==4.3.0 imbalanced-learn shap
```

Core libraries: `scikit-learn` · `xgboost` · `pandas` · `numpy` · `matplotlib` · `seaborn` · `joblib` · `ipywidgets`

---

## 🔍 SHAP Explainability

SHAP (SHapley Additive exPlanations) is used to identify which flow-level features contribute most to the model's attack/benign decisions. The top 15 features by mean absolute SHAP value are plotted and saved as `shap_importance.png`.

---

## 🖥️ Dashboard

A static HTML dashboard is hosted via GitHub Pages at:

```
https://Bennythomas333.github.io/final-year-project-/dashboard/index.html
```

---

## 📄 License

This project is for academic purposes only.

